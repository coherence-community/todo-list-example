micronaut:
  application:
    name: todoListMicronautServer
  server:
    port: 3000
  router:
    static-resources:
      default:
        enabled: true
        mapping: "/**"
        paths: "classpath:web"
